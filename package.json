{
  "name": "chakra-manifesto",
  "version": "1.0.0",
  "description": "WHIP Manifesto - Software is a language now",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./docs/output.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./docs/output.css --watch",
    "build": "npm run build:css && cp src/index.html docs/index.html"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
